import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins:  ['motherboard-degrees-agreement-nokia.trycloudflare.com'],
};

export default nextConfig;
